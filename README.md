# GHOSTLAB-ANALYTICS-MODELS

Local-first analytics + ML pipelines for Ghost Laboratory.

Goals:
- Run models locally (Mac) with cached parquet datasets
- Deterministic runs + saved artifacts
- Optional export/import to cloud later
